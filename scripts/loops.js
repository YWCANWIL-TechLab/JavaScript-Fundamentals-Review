// * LOOP EXERCISES


/**
 * Given an array of Numbers called arr, loop through the array using a regular
 * for loop and find the sum of all elements in the array.
 * 
 * Return the sum.
 * 
 * @param {Number[]} arr 
 * @returns {Number}
 */
function sumOfArray(arr) {
    // TODO
}


/**
 * Given an array of Numbers called arr, loop through the array using a for/of
 * loop and find the sum of all elements in the array.
 * 
 * Return the sum.
 * 
 * @param {Number[]} arr 
 * @returns {Number}
 */
function sumOfArrayForOf(arr) {
    // TODO
}


/**
 * Given an array of Strings called stringArr, loop through the array and keep
 * track of the number of characters in each string. 
 * 
 * Return the total number of characters in the array.
 * 
 * @param {String[]} stringArr 
 */
function numCharactersInArray(stringArr) {
    // TODO
}


/**
 * Given a Number called num, print out every number between 1 and the number
 * that is divisible by 2.
 * 
 * @param {Number} num 
 */
function divisibleBy2(num) {
    // TODO
}


/**
 * Given a Number called n, print out every number between 0 and n (inclusive).
 * 
 * 
 * @param {Number} n 
 */
function printNumbersUpToN(n) {
    // TODO
}


/**
 * Given an Object called houses that represents a list of houses, find the 
 * house with the highest property value. 
 * 
 * Return the highest property value.
 * 
 * @param {Object} houses 
 * @returns {Number}
 */
function findLargestPropertyValue(houses) {
    // TODO
}



/**
 * Given a String called prop that represents a possible property of an object,
 * return that property's value if it exists. If there is no property of that
 * name in the object, return 'No property found'.
 * 
 * @param {Object} obj
 * @param {String} prop 
 * @returns {String}
 */
function findProperty(prop, obj) {
    // TODO
}


/**
 * Given a number called n, print out every number that is less than n * 2 and
 * greater than -1. I should always print out at least one number.
 * 
 * @param {Number} n 
 */
function doWhile(n) {
    // TODO
}
