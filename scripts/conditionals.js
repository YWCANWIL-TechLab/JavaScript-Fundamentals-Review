// * CONDITIONALS


/**
 * Given a boolean called completedFrontEnd which represents whether someone
 * has completed front end web development, if they have completed it return
 * "Certificate Complete!". If not, return "Almost there!"
 * 
 * @param {Boolean} completedFrontEnd 
 * @returns {String}
 */
function certificate(completedFrontEnd) {
    // TODO
}


/**
 * Given two numbers called num1 and num2, return the larger of the two numbers. Do
 * not use Math.max().
 * 
 * @param {Number} num1 
 * @param {Number} num2 
 * @returns {Number}
 */
function greaterThan(num1, num2) {
    // TODO
}


/**
 * Given a Number called num, check if the number is even or odd. If the number
 * is even, return "This number is even". If the number is odd, return "This 
 * number is odd".
 * 
 * @param {Number} num 
 * @returns {String}
 */
function isEven(num) {
    // TODO
}


/**
 * Given a String called str, check if the String contains any vowels. For this
 * exercises, vowels are considered to be 'a', 'e', 'i', 'o', 'u'.
 * 
 * Return true if the string contains any vowels or false if it does not.
 * 
 * @param {String} str 
 * @returns {Boolean}
 */
function containsVowels(str) {
    // TODO
}


/**
 * Given a string called str, return true if the string contains any numbers (0
 * - 9) or false if it does not.
 * 
 * @param {String} str 
 * @returns {Boolean}
 */
function containsNumbers(str) {
    // TODO
}



/**
 * Given a number 1 - 7 called num that represents the day of the week, return 
 * the day of the week as a string. If the number is not between 1 and 7, return
 * 'Invalid Input'.
 * 
 * Note: There a multiple ways to do this
 * 
 * @param {Number} num 
 * @returns {String}
 */
function dayOfTheWeek(num) {
    // TODO
}


/**
 * Given a Number 1 - 12 called num, return the String 'I am in nth grade' for
 * each num. So for 1, you would return 'I am in first grade' and for 2 you 
 * would return 'I am in second grade', etc. If the number is not between 1 and
 * 12 (inclusive), return 'Invalid Grade'.
 * 
 * @param {Number} num 
 * @returns {String}
 */
function whatGradeAmI(num) {
    // TODO
}




