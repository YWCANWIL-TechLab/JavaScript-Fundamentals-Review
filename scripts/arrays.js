// * ARRAY REVIEW
// ! None of these will require a loop

/**
 * Given an array of numbers, print out all the elements in the array. You can
 * assume there will be three elements in the array. You do not have to use a 
 * loop if you know the number of elements, however you are welcome to do that
 * if you would like.
 * 
 * @param {Number[]} arr 
 */
function printElementsOfArray(arr) {
    // TODO
}


/**
 * Given an array of numbers, add num to the array. Please add it to the end of
 * the array.
 * 
 * Return the array.
 * 
 * @param {Number} num 
 * @param {Number[]} arr 
 * @returns {Number[]}
 */
function addNumToArray(num, arr) {
    // TODO
}


/**
 * Given an array of numbers called arr and a number called num, add num to the 
 * beginning of arr.
 * 
 * Return the array.
 * 
 * @param {Number} num 
 * @param {Number[]} arr 
 * @returns {Number[]}
 */
function addNumToArrayStart(num, arr) {
    // TODO
}


/**
 * Given a number called num and an array of numbers called arr, find the index
 * of num of num in the array. If it does not exist in the array, return -1. 
 * 
 * @param {Number} num 
 * @param {Number[]} arr 
 */
function findIndexOfElement(num, arr) {
    // TODO
}


/**
 * Given a number called num and an array of number called arr, find and return
 * the number num in the array arr if it exists, otherwise return -1.
 * 
 * @param {Number} num 
 * @param {Number[]} arr 
 * @returns {Number}
 */
function findElementInArray(num, arr) {
    // TODO
}


/**
 * Given an array of numbers called arr and a number called num, remove the given
 * num from the array. If the array does not contain num, return -1.
 * 
 * @param {Number} num 
 * @param {Number[]} arr 
 * @returns {Number[] || Number}
 */
function removeElement(num, arr) {
    // TODO
}


/**
 * Given two numbers and an array of numbers, numToReplace, replacedWith, and 
 * arr, replace numToReplace inside of arr with replacedWith. You will need to
 * search the array for numToReplace. Return the new array or -1 if the array
 * does not contain numToReplace
 * 
 * Note: You do not need a loop to solve this. If you're stuck take a peak at
 * the different array methods you have access to.
 * 
 * @param {Number} numToReplace 
 * @param {Number} replacedWith 
 * @param {Number[]} arr 
 * @returns {Number[] || Number}
 */
function replaceElement(numToReplace, replacedWith, arr) {
    // TODO 
}


/**
 * Given two numbers called num1 and num2 and an array of numbers, return a
 * subarray of number between num1 and num2.
 * 
 * @param {Number} num1 
 * @param {Number} num2 
 * @param {Number[]} arr 
 */
function elementsBetweenNum1AndNum2(num1, num2, arr) {
    // TODO
}


